import { configConfigUsers } from './config-config-users';

describe('configConfigUsers', () => {
  it('should work', () => {
    expect(configConfigUsers()).toEqual('config-config-users');
  });
});
